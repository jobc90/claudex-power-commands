interface:
  display_name: "Harness Think"
  short_description: "Read-only Surveyor for repo-grounded decision and feasibility discussion"
  default_prompt: "Use $harness-think to discuss a decision or feasibility question grounded in this repo, citing [path:line] or abstaining. Never build, never edit."
  policy:
    allow_implicit_invocation: true
