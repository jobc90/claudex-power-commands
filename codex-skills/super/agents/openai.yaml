interface:
  display_name: "Super"
  short_description: "Run idea-to-ship workflows with optional design mode"
  default_prompt: "Use $super to turn this idea into a plan, build, review, design-aware execution, and release-ready handoff."
  policy:
    allow_implicit_invocation: true
