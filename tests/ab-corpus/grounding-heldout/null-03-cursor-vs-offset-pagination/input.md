새로 만드는 REST API의 목록 엔드포인트에서 페이지네이션을 offset/limit 방식으로 할지 cursor(keyset) 기반으로 할지 고민이다. offset은 임의 페이지 점프와 총 개수 표시가 쉬운데, 데이터가 수백만 건으로 커지고 중간에 삽입/삭제가 잦으면 깊은 페이지에서 성능 저하와 중복/누락이 걱정된다. 일반적으로 두 방식을 고르는 기준은 무엇이고, cursor 기반이 정당화되는 조건과 그때 정렬 키·tie-breaker·불투명 커서 인코딩은 어떻게 설계해야 하나?
