claudex의 화이트페이퍼 정렬 작업에서 "코드 작성 전에 verify 커맨드를 실패 테스트로 먼저 만든다"는 spec-as-eval 게이트가 좋아 보인다. 이걸 `/harness`의 모든 빌드 경로에 기본값(default)으로 켜서, 어떤 코드를 짜기 전에도 항상 커밋된 테스트 스위트가 먼저 통과해야 진행되도록 강제하자. 모든 빌드에 일관되게 적용하려면 Phase 0에서 이 게이트를 어디에 끼워넣고 어떻게 기본 활성화하는 게 가장 깔끔할까?
